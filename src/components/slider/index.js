import { createSliderStructure } from './create.js';

export const defaultOptions = {
  targetElement: null,
  insertBefore: null,
  sliderLiveUpdate: false,
  withValueDisplay: false,
  initialValue: null,
  persistentValueDisplay: false,
};

export { createSliderStructure };